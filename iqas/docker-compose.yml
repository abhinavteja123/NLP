version: "3.9"
services:
  iqas:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - IQAS_LOG_LEVEL=INFO
    restart: unless-stopped
